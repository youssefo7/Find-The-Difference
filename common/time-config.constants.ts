import { MAX_MS_TIME, MS_PER_SECOND } from './constants';

export const DEFAULT_TOTAL_TIME = 30;
export const DEFAULT_PENALTY_TIME = 5;
export const DEFAULT_REWARD_TIME = 5;
export const MIN_TIME_JUMP = 0;
export const MAX_TIME_JUMP = 60;
export const MIN_GAME_TIME = 10;
export const MAX_GAME_TIME = MAX_MS_TIME / MS_PER_SECOND;
